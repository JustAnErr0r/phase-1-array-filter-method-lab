//existing array = drivers
// takes array of names and a string as arguements to return list of drivers

// function findMatching(array, string) {
//     return drivers === string
// }

// console.log(findMatching(drivers, "Bobby"))
const copies = drivers.filter(function findMatching(drivers, string) {
    return drivers === string
  })
